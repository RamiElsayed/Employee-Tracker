const {describe, expect, test, it} = require('@jest/globals');
const { Department } = require("../models/department");
jest.mock("Department")

describe('Department',() => {
   
it("This should retrun the value passed to it"),() => {

}
})